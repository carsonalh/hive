# Hive Core Library

This is a library that is used by both the client frontend and the server
backend that implements the game logic for the hive game.

On the frontend, this compiles to a WebAssembly module and on the backend to a
shared library.

