import Component from "./component";

/**
 * Just a marker that a tile is a preview tile and should (in some circumstances) be treated
 * specially.
 */
export default class TilePreviewComponent extends Component {}